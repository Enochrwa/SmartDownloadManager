{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main SmartDownloadManager window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default", "notification:default"]
}
