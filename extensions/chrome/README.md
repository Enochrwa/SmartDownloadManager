# Chrome Extension (Manifest V3)

Scoped for Phase 2, Sprint 8 (`docs/SPRINT_PLAN.md`). Will share a TypeScript
core with `extensions/firefox` — download interception, clipboard monitoring,
drag & drop, batch URL detection — talking to the desktop app's REST/WS API
(`crates/server`'s `sdmd` daemon).
