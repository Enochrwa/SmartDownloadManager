[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "cargo xtask: cross-platform dev/release automation (replaces shell scripts that don't run on Windows)."

[dependencies]
anyhow.workspace = true
clap.workspace = true
